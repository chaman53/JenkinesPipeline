pipeline {
    agent any
    
    stages {
        stage('test') {
            steps {
               // sh label: '', script: 'ls'
                sh 'ls'
            }
          }
      stage('build') {
            steps {
                sh label: '', script: 'ls'
        }
    }
}
}
