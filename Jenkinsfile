pipeline {
    agent any
    
    stages {
        stage('test') {
            steps {
                sh label: '', script: 'ls'
            }
          }
      stage('build') {
            steps {
                sh label: '', script: 'ls'
        }
    }
}
}
